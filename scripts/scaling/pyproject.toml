[project]
name = "duntale-scaling"
version = "0.1.0"
description = "Asset parser and scaling generator for Duntale dungeon progression"
requires-python = ">=3.11"
# No external dependencies — uses only stdlib (json, sqlite3, pathlib, math)
dependencies = [
    "rich>=14.3.3",
]
